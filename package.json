{
  "name": "roost",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Roost — a local-first Agent Development Environment (Phase 0 foundation)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "smoke": "pnpm build && tsx scripts/smoke.ts"
  },
  "devDependencies": {
    "@roost/contracts": "workspace:*",
    "@roost/core": "workspace:*",
    "@types/node": "^24.13.4",
    "tsx": "^4.23.13",
    "typescript": "~5.9.3",
    "vitest": "^3.2.7"
  }
}
